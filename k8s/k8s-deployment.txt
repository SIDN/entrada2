./mvnw spring-boot:build-image


# Rancher

# Create Serviceaccount

# Create RBAC

## ClusterRole 

## ClusterRolBinding


## Create deployment

Properties:
 
- AWS_REGION
- maxmind.license.paid
- spring_profiles_active 
- SPRING_CLOUD_BOOTSTRAP_ENABLED
- configmap

name of configmap must match: spring.application.name


## Create service

Create service for controller, so worker can cannot to it



## Create ingress

create ingress for controller service, so external client can upload pcap
 set an annotation on your ingress like this: nginx.ingress.kubernetes.io/proxy-body-size: "0"
 
